Run `lazy yolo $ARGUMENTS`. If a PRD file path is provided, start yolo mode — this parses the PRD into sprints and returns a master prompt for autonomous execution. If no arguments are given, run `lazy yolo status` to show current progress. Follow the master prompt instructions exactly: execute each sprint's tasks, validate with `lazy_check`, and advance with `lazy_yolo_advance` until all sprints are complete. Never stop to ask for confirmation.